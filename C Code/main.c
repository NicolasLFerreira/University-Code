#include <stdio.h>
#include <stdlib.h>
#include "functions.h"

void main()
{
    while (true)
    {
        lowerToUpper();
    }

    // int number = 5;
    // int *numberPtr = &number;

    // int abc = 123;
    // char buffer[10];
    // sprintf(buffer, "%d", abc);

    // printf(buffer);
}